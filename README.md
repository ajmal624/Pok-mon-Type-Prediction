#Pokémon Type Prediction

Unzip the File
use internet for all installation

Install Python 3.14, then verify the installation:
    Press Windows + R, type `cmd`, and press Enter.
    In Command Prompt, run:
        python --version

Open Visual Studio Code and install the required Python extensions.

Go to File → New Window → Open Folder, then select the `Pokemon Type Prediction` folder.

Open the terminal and run:
    py -m venv venv
    venv\Scripts\Activate
    pip install -r requirements.txt

Run the application:
    streamlit run streamlit_app.py
